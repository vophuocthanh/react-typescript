import React from "react";

const Card = ({
  title,
  desc,
  image,
  link,
}: {
  title: string;
  desc: string;
  image: string;
  link: string;
}) => {
  return <div>Card</div>;
};

export default Card;
